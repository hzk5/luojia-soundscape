server:
  port: 8503
spring:
  application:
    name: luojia-soundscape-user
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${MYSQL_HOST:127.0.0.1}:${MYSQL_PORT:3306}/luojia_soundscape_user?serverTimezone=Asia/Shanghai&characterEncoding=utf8&useUnicode=true&useSSL=false
    username: ${MYSQL_USERNAME}
    password: ${MYSQL_PASSWORD}
wx:
  miniapp:
    appid: ${WECHAT_APP_ID}
    secret: ${WECHAT_APP_SECRET}
    msgDataFormat: JSON
