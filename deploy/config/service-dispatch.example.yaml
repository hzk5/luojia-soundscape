server:
  port: 8509
spring:
  application:
    name: luojia-soundscape-dispatch
xxl:
  job:
    admin:
      addresses: ${XXL_JOB_ADMIN_URL:http://127.0.0.1:8080/xxl-job-admin}
    accessToken: ${XXL_JOB_ACCESS_TOKEN}
    executor:
      appname: luojia-soundscape-dispatch
      port: 9999
      logpath: ${XXL_JOB_LOG_PATH:./logs/xxl-job}
      logretentiondays: 30
